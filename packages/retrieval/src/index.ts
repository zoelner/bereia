/**
 * Barrel do pacote `@bereia/retrieval` (N2). Ponto único de exportação —
 * N6 (composição `PgRetrieval`) é a próxima e única edição pós-G2 deste
 * arquivo (plano §6, "Notas de disjunção").
 */
export * from "./embedder.js";
